/**
 * @enum {string}
 * @description Enum for the different panel tabs.
 */
export enum PanelTab {
    OVERVIEW = 'overview',
    PROMPT = 'prompt',
    SUMMARIZER = 'summarizer',
    TRANSLATOR = 'translator',
    DETECTOR = 'detector',
    PROOFREADER = 'proofreader',
    WRITER = 'writer',
    REWRITER = 'rewriter',
    MODELS = 'models',
}
