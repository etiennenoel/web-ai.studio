/** How the matrix groups its rows. */
export enum MatrixGroupingEnum {
  /** By the kind of machine a reader would buy. */
  Type = 'type',
  /** By how the machine gets its memory bandwidth. */
  Family = 'family',
}
